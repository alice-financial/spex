<ul>
  <% if user_signed_in? %>
    <li>
      <%= link_to(t('devise.links.log_out'), destroy_user_session_path, :method => :delete) %>
    </li>
  <% else %>
    <li>
      <%= link_to(t('devise.links.log_in'), new_user_session_path)  %>
    </li>
  <% end %>
</ul>